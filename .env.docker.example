POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_THE_PASSWORD
POSTGRES_DB=in-memory-db
POSTGRES_HOST_PORT=5434

API_PORT=8080

# DATABASE_URL and KV_ADDR are set in docker-compose.yml for api/migrate/seed
# (postgres:5432 and kv:55555 on the Docker network).
